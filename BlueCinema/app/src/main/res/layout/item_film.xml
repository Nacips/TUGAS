<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:background="@drawable/bg_card"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- GAMBAR FILM -->
    <ImageView
        android:id="@+id/imgFilm"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_gravity="center"
        android:background="#16213E"
        android:scaleType="centerCrop"
        android:src="@drawable/reze" />

    <!-- WRAPPER CONTENT -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:orientation="vertical">

        <!-- JUDUL FILM -->
        <!-- dibuat fix 2 baris agar semua card sejajar -->
        <TextView
            android:id="@+id/tvFilmTitle"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:ellipsize="end"
            android:maxLines="2"
            android:text="Chainsaw Man The Movie: Reze Arc"
            android:textColor="@color/white_text"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"/>

        <!-- INFO STUDIO DAN JAM -->
        <TextView
            android:id="@+id/tvFilmInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Studio 1 • 19:00"
            android:textColor="@color/gray_text"
            android:textSize="13sp" />

        <!-- HARGA -->
        <TextView
            android:id="@+id/tvFilmPrice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Rp55.000"
            android:textColor="@color/blue_soft"
            android:textSize="16sp"
            android:textStyle="bold" />

        <!-- BUTTON -->
        <Button
            android:id="@+id/btnAddFilm"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="14dp"
            android:background="@drawable/bg_button_primary"
            android:text="🎬 Pilih Film"
            android:textAllCaps="false"
            android:textColor="@color/white_text"
            android:textSize="14sp"
            android:textStyle="bold" />

    </LinearLayout>

</LinearLayout>